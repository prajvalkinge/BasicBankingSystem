<%- include("partials/header") %>

<div class="transfer-header">
    <h1>RECIVER </h1>
</div>

<div class="table table-dark">
    <table>
        <tr>
            <th>Customer Name</th>
            <th>Email</th>
            <th>Credits</th>
            <th></th>
        </tr>
    <% for(let current of users){ %>

        <% if(!user._id.equals(current._id)){ %>
            <tr>
                <td><%= current.name %></td>
                <td><%= current.email%></td>
                <td><%= current.credits%></td>
                <td><button class="select-btn"><a href="/sender/<%= user._id %>/<%= current._id %>">send >></a></button></td>
            </tr>
        <% } %>

    <% } %>
    </table>    
</div>

<%- include("partials/footer") %>